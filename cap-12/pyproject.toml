[project]
name = "market-intelligence-system-cap12"
version = "0.1.0"
requires-python = ">=3.11"
# cap-12 e exclusivamente de infraestrutura (Docker/Railway)
# sem codigo Python para instalar
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
